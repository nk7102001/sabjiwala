<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit Product | Sabjiwala Seller Panel</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col font-sans">

  <!-- Navbar -->
  <%- include('../partials/navbar') %>

  <!-- Main Content -->
  <main class="flex-grow max-w-4xl mx-auto p-6" role="main" aria-labelledby="pageTitle">
    <!-- Header -->
    <div class="bg-gradient-to-r from-blue-600 to-blue-400 p-6 rounded-lg shadow-lg mb-8">
      <h1 id="pageTitle" class="text-2xl sm:text-3xl font-extrabold text-white">✏️ Edit Product</h1>
      <p class="text-blue-50 mt-1 text-sm">Update your product information below.</p>
    </div>

    <!-- Edit Form -->
    <div class="bg-white p-6 rounded-xl shadow-md hover:shadow-lg transition border border-gray-100">
      <form action="/seller/products/edit/<%= product && product._id %>" method="POST" enctype="multipart/form-data" class="space-y-5" novalidate>

        <!-- Product Name -->
        <div>
          <label for="name" class="block font-semibold text-gray-700 mb-1">Product Name</label>
          <input
            type="text"
            id="name"
            name="name"
            value="<%= (product && product.name) ? product.name : '' %>"
            required
            class="border border-gray-300 w-full p-3 rounded focus:outline-none focus:ring-2 focus:ring-blue-400"
          />
        </div>

        <!-- Description -->
        <div>
          <label for="description" class="block font-semibold text-gray-700 mb-1">Description</label>
          <textarea
            id="description"
            name="description"
            rows="3"
            class="border border-gray-300 w-full p-3 rounded focus:outline-none focus:ring-2 focus:ring-blue-400"><%= (product && product.description) ? product.description : '' %></textarea>
        </div>

        <!-- Category -->
        <div>
          <label for="category" class="block font-semibold text-gray-700 mb-1">Category</label>
          <input
            type="text"
            id="category"
            name="category"
            value="<%= (product && product.category) ? product.category : '' %>"
            required
            class="border border-gray-300 w-full p-3 rounded focus:outline-none focus:ring-2 focus:ring-blue-400"
          />
        </div>

        <!-- Price & Qty -->
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
          <div>
            <label for="pricePerKg" class="block font-semibold text-gray-700 mb-1">Price Per Kg (₹)</label>
            <input
              type="number"
              id="pricePerKg"
              name="pricePerKg"
              step="0.01"
              min="0"
              value="<%= (product && product.pricePerKg) ? product.pricePerKg : 0 %>"
              required
              class="border border-gray-300 w-full p-3 rounded focus:outline-none focus:ring-2 focus:ring-blue-400"
            />
          </div>
          <div>
            <label for="availableQty" class="block font-semibold text-gray-700 mb-1">Available Qty (Kg)</label>
            <input
              type="number"
              id="availableQty"
              name="availableQty"
              step="0.1"
              min="0"
              value="<%= (product && product.availableQty) ? product.availableQty : 0 %>"
              required
              class="border border-gray-300 w-full p-3 rounded focus:outline-none focus:ring-2 focus:ring-blue-400"
            />
          </div>
        </div>

        <!-- Current Image -->
        <div>
          <label class="block font-semibold text-gray-700 mb-1">Current Image</label>
          <% if (product && product.imageUrl) { %>
            <img
              src="<%= product.imageUrl %>"
              alt="<%= product.name || 'Product image' %>"
              class="w-40 h-40 object-cover rounded border mb-3">
          <% } else { %>
            <p class="text-gray-500 text-sm mb-3">No image uploaded.</p>
          <% } %>
        </div>

        <!-- Upload New Image -->
        <div>
          <label for="image" class="block font-semibold text-gray-700 mb-1">Change Image (optional)</label>
          <input
            type="file"
            id="image"
            name="image"
            accept="image/*"
            class="border border-gray-300 w-full p-3 rounded focus:outline-none focus:ring-2 focus:ring-blue-400 file:mr-4 file:py-2 file:px-4 file:rounded file:border-0 file:bg-blue-50 file:text-blue-700 hover:file:bg-blue-100"
          />
        </div>

        <!-- Submit Button -->
        <div class="pt-4">
          <button
            type="submit"
            class="w-full bg-blue-500 hover:bg-blue-600 text-white font-semibold py-3 rounded-lg shadow-md hover:shadow-lg transition transform hover:-translate-y-0.5"
          >
            💾 Save Changes
          </button>
        </div>

      </form>
    </div>
  </main>

  <!-- Footer -->
  <%- include('../partials/footer') %>

</body>
</html>
