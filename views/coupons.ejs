<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Coupons - Sabjiwala</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">
  <%- include('partials/navbar') %>

  <main class="flex-grow max-w-4xl mx-auto px-6 py-16">
    <h1 class="text-4xl font-extrabold text-green-700 mb-10">Available Coupons</h1>

    <% if (coupons.length === 0) { %>
      <p class="text-gray-700">No coupons available right now. Please check back later!</p>
    <% } else { %>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <% coupons.forEach(coupon => { %>
          <div class="bg-white p-6 rounded shadow-md border border-green-200">
            <h2 class="text-2xl font-semibold mb-2"><%= coupon.code %></h2>
            <p class="mb-2 text-gray-700"><%= coupon.description || 'No description' %></p>
            <p class="mb-2 font-bold text-green-600">
              Discount: 
              <% if (coupon.discountType === 'percentage') { %>
                <%= coupon.discountValue %>% off
              <% } else { %>
                ₹<%= coupon.discountValue %> off
              <% } %>
            </p>
            <p class="text-sm text-gray-500">
              Valid until: <%= coupon.expiryDate.toLocaleDateString('en-IN') %>
            </p>
          </div>
        <% }) %>
      </div>
    <% } %>
  </main>

  <%- include('partials/footer') %>
</body>
</html>
